# hypridle — solo eventos de sueño/tapa. SIN timers de inactividad
# (decisión del usuario: bloquear solo al cerrar la tapa o con Super+L)

general {
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on
}
